# @package _global_.generator
_target_: generator.ddpm.DDPM
# Note: changed this from ${model} to ${experiment.model}. Does that make a difference?
model: ${experiment.model}  # This will be instantiated first
betas: [1e-4, 0.02]
n_T: 400
drop_prob: 0.1 
noise_shape: ${dataset.data_shape}