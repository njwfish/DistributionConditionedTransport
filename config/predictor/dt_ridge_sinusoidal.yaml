# @package _global_.predictor
_target_: predictor.dt_conditioned_predictor.DTConditionedRidgePredictor
latent_dim: ${experiment.latent_dim}
ridge_alpha: 1e-3
conditioning_mode: "sinusoidal"
dt_embed_dim: 16
normalize_dt: true
film_hidden_dim: 64  # Not used in sinusoidal mode but kept for consistency 