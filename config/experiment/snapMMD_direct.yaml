# @package _global_.experiment

# Experiment name and description
name: snapMMD_direct_${dataset_name}
description: "SnapMMD ${dataset_name} experiment with unified dataset class"

# Override defaults from config.yaml
defaults:
  - /dataset: snapMMD_unified
  - /encoder: knn
  - /model: diffusion_mlp
  - /generator: flow_matching
  - /training: default5000
  - /loss: predictor
  - /predictor: default
  - /sampling: default


# predictor parameters
predictor_loss_weight: 1
predictor_cotrain_direct: true  # If true, use predictor output as target_latent for generator instead of adding loss term

train_predictor_posthoc: false

#predictor_model_type: "ridge"
#predictor_model_args:
#  ridge_alpha: 1e-3

predictor_model_type: "mlp"
predictor_model_args:
  hidden_dim: 128
  num_layers: 2

# sampling parameters
num_samples: null
selective_pairing_mode: null

# Experiment-specific parameters
latent_dim: 32
hidden_dim: 128
set_size: 32
batch_size: 8
lr: 2e-4