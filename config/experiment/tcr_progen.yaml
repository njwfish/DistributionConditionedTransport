# @package _global_.experiment

# Experiment name and description
name: tcr_progen
description: "TCR repertoire dataset with Progen2 generator"

# Override defaults from config.yaml
defaults:
  - /dataset: tcr
  - /encoder: esm
  - /generator: progen
  - /scheduler: cosine
  - /loss: default
  - /sampling: default
  - /training: default5000_mem
  - /coupling: edit_distance

# Predictor settings
train_predictor_posthoc: true

# Sampling parameters
num_samples: 50
selective_pairing_mode: null

# Model dimensions
latent_dim: 128
hidden_dim: 256

# Training/data sizing
batch_size: 1
set_size: 512
lr: 2e-5

# ESM encoder settings
esm_model_name: 'facebook/esm2_t6_8M_UR50D'
freeze_esm2: false

# Progen2 generator settings
progen2_model_name: 'hugohrban/progen2-small'
max_length_progen: 16 # here we have 16 instead of 18, due to generator sampling setup

# Dataset behavior
within_patient: false  # True: only consecutive timepoints within patients; False: any-to-any
split: train  # 'train' or 'test'
test_fraction: 0.0  # Fraction of patients to hold out for testing
