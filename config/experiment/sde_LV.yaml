# @package _global_.experiment

# Experiment name and description
name: snapMMD_classic_sde_LV_exp
description: "SnapMMD classic SDE LV experiment with coupled distribution embeddings"

# TODO: usin diffusion/flow matching for now as a first iteration.
# Override defaults from config.yaml
defaults:
  - /dataset: snapMMD_classic_sde_LV
  - /encoder: gnn
  - /model: diffusion_mlp
  - /predictor: mlp
  - /loss: predictor
  - /generator: flow_matching  # Use flow matching for coupled embeddings
  - /training: default5000
  - /collate: shift_pairing  # Use random pairing for source/target

# TODO: need to change these parameters for PBMC?
# Experiment-specific parameters
latent_dim: 16
hidden_dim: 64
set_size: 500
batch_size: 256
lr: 2e-4