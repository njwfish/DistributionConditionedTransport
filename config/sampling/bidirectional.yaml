# @package _global_.sampling

# Bidirectional sampling - equal weights for all samples (ignores dt values)
mode: "bidirectional"
num_samples: null  # Use dataset length
replacement: true
const_weight: 1.0 