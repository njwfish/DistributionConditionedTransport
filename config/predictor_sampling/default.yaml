# @package _global_.predictor_sampling
_target_: utils.custom_sampler.CustomWeightedSampler
weight_mode: uniform
num_samples: ${experiment.num_samples}
replacement: true
const_weight: 1.0 
selective_pairing_mode: "single_step"
exponential_weight_scale: ${experiment.exponential_weight_scale}