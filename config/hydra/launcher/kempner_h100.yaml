# @package hydra.launcher
# Kempner H100 partition launcher config
_target_: hydra_plugins.hydra_submitit_launcher.submitit_launcher.SlurmLauncher

# SLURM partition and account
partition: kempner_h100
account: kempner_mzitnik_lab

# Resource allocation (H100 nodes: 4 GPUs, 96 cores, 1.5TB RAM)
gpus_per_node: 1
cpus_per_task: 12
mem_gb: 96
timeout_min: 360  # 6 hours

# Submitit settings
submitit_folder: ${hydra.sweep.dir}/.submitit/%j
array_parallelism: 16  # Max concurrent jobs
nodes: 1
tasks_per_node: 1
max_num_timeout: 3

# GRES specification
gres: gpu:nvidia_h100_80gb_hbm3:1

# Additional SLURM parameters
additional_parameters:
  mail-type: END,FAIL
  mail-user: ${oc.env:USER}@g.harvard.edu


