# @package _global_.sampling

# Exponential sampling - weight = exp(|dt|) / ln(2)
mode: "exponential"
num_samples: ${experiment.num_samples}
replacement: true 
const_weight: 1.0  # Not used in exponential mode, but kept for consistency 