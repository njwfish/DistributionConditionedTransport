# @package _global_.experiment

# Experiment name and description
name: mvn_coupled_exp
description: "Multivariate normal distribution experiment with coupled distribution embeddings"

# Override defaults from config.yaml
defaults:
  - /dataset: mvn
  - /encoder: gnn
  - /model: diffusion_mlp
  - /generator: flow_matching
  - /training: default5000
  - /sampling: default

# sampling parameters
weight_mode: "uniform"
num_samples: 50   # number of pairs drawn from dataset each epoch
unidirectional: false
exponential_weight_scale: 1.0

# Experiment-specific parameters
latent_dim: 16
hidden_dim: 64
set_size: 500
batch_size: 256
lr: 2e-4 