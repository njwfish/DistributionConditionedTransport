# @package _global_.experiment
# Base GMM experiment with EmbeddingEncoder - generator must be specified separately

name: batchint_onehot_mmd
description: "Batch integration"

# Override defaults - uses embedding encoder instead of GNN
defaults:
  - /dataset: batch_integration
  - /encoder: embedding
  - /model: direct_mlp
  - /generator: wasserstein
  - /scheduler: cosine
  - /training: default5000
  - /loss: multimarginal

# Experiment-specific parameters
latent_dim: 16
hidden_dim: 256
set_size: 512
batch_size: 256
lr: 2e-4