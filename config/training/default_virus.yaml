# @package _global_.training
_target_: training.Trainer
num_epochs: 1000
log_interval: 10
save_interval: 10
eval_interval: 10
early_stopping: false
patience: 5
use_tqdm: false
gradient_accumulation_steps: ${experiment.gradient_accumulation_steps} 
use_esm_tokens: ${experiment.use_esm_tokens}
use_amp: true