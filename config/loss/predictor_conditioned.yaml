# @package _global_.loss
# Conditioned predictor loss manager
# - Concatenates cell_cond with input features for encoder (43 -> 45 dim)
# - Passes treat_cond to the conditioned predictor
_target_: loss.predictor_conditioned.PredictorConditionedLossManager
predictor_loss_weight: ${experiment.predictor_loss_weight}
