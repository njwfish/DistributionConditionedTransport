# @package _global_.model
_target_: model.multimarginal.MultiMarginalMLP
data_dim: ${dataset.data_shape[0]}
latent_dim: ${experiment.latent_dim}
hidden_dim: ${experiment.hidden_dim}
out_dim: ${dataset.data_shape[0]}
layers: 4