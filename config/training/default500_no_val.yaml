# @package _global_.training
_target_: training.Trainer
num_epochs: 500
log_interval: 50
save_interval: 100
eval_interval: 100
early_stopping: true
patience: 20
use_tqdm: false

# No validation split - use full dataset for training (legacy behavior)
validation_split: 0.0
shuffle_before_split: true 